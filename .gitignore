# Editor / IDE / local tooling
.vscode/
.idea/
.claude/
*.swp

# OS
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Node (the project itself has no dependencies; keep stray installs out)
node_modules/
npm-debug.log*
package-lock.json

# Logs and scratch files
*.log
*.tmp
*.bak

# Files the app writes at runtime — never source
ENIGMA-*-Updated.html
ENIGMA-*-activity.json
